@model Assignment

@{
    ViewData["Title"] = "Create";
}
@*right now the issue is that the modelstate isnt finding the course assicoated with the assignment*@

@*I am trying to go from an action with specfic id to another controller action with a linked id to orginal model(creating new item with reference to orginally object)*@
<div class="row mt-4 text">
    <div class="col-md-8 offset-3">
        <h1>Add Assignment</h1>
        <div class="row">
            <div class="col-8">
                <form asp-action="Create" asp-controller="Assignment">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group text-center">
                        <div class="form-group">
                            <label class="float-start">Course</label>
                            <input class="form-control" value="@ViewBag.CourseName" disabled/>
                            <input type="hidden" class="form-control" asp-for="CourseID"/>
                        </div>
                        <div class="form-group">
                            <label asp-for="AssignmentName" class="float-start">Assignment</label>
                            <input asp-for="AssignmentName" class="form-control"/>
                            <span asp-validation-for="AssignmentName" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="DueDate" class="float-start">Due Date</label>
                            <input asp-for="DueDate" class="form-control"/>
                            <span asp-validation-for="DueDate" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="MaxPoints" class="float-start">Total Points</label>
                            <input asp-for="MaxPoints" class="form-control"/>
                            <span asp-validation-for="MaxPoints" class="text-danger"></span>
                        </div>
                        <input type="hidden" asp-for="Course" />
                        
                        <div class="form-group mt-2">
                            <input type="submit" value="Create" class="btn btn-outline-success float-end"/>
                            <a class="btn btn-outline-danger float-start" asp-controller="Course" asp-action="Index">Show All</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@*
<form method="post">
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Add Assignment</h2>
            <hr />
        </div>

        <button type="submit" value="Create" class="btn btn-primary" style="width: 150px;">Create</button>
        <a asp-controller="Course" asp-action="Index" class="btn btn-secondary" style="width:150px;">Back to Course</a>
    </div>
</form>
*@
@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}


